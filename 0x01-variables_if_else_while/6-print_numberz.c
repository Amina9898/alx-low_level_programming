#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - entry point
 * description: this program will
 * Return: return always 0
 */

int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
		putchar(n);
	putchar('\n');

	return (0);
}
