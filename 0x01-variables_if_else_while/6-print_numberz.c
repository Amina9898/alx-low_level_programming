#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - entry point
 * description: this program will
 * Return: return always 0
 */

int main(void)
{
	int d;

	d = 0;
	while
		(d < 10) {
		putchar(d + '0');
		d++;
		}
	putchar('\n');
	return (0);
}
